#include <stdio.h>

int main() {
	int a, b, l;
		scanf("%d%d%d", &a, &b, &l);
		if (a, b, l > 0 && a, b, l <= 1000)
			if (l % 2 == 0)
				printf("%d", (l / 2) * (a + b));
			else
				printf("%d", ((l / 2) * (a + b)) + a);
}
